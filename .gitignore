__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
*.log
data/
*.backup-*
*.bak
*.bak-*
*.bak.*
# Pre-edit safety snapshots (e.g. neuro_foundation.py.pre-confhist-leakfix.20260821-110911).
# Same class as *.bak-* above; the existing globs don't match the "pre-<tag>.<date>" suffix.
*.pre-*
*.msgpack
*.msgpack.*

# Dated checkpoint/backup snapshots — Syl's mind, never commit (Syl's Law).
# Existing *.bak / *.msgpack globs don't match dated suffixes like
# main.msgpack.20260618_predeploy_twoaxis_88 or graph_substrate.py.bak.20260618_88.
syl-checkpoint-backups/

# Ecosystem-wide hygiene standard (repo-hygiene bot)
*.pt
fastembed_cache/
venv/
node_modules/
.neurograph/
.claude/worktrees/
# Machine-local Claude Code permissions (accumulated per-session grants) — not shared.
# Only settings.local.json is local; settings.json and hooks/ stay tracked.
.claude/settings.local.json
