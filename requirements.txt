numpy>=1.24.0
scipy>=1.10.0
msgpack>=1.0.0
sentence-transformers>=3.0.0,<6.0.0
transformers>=4.41.0
# ---- Changelog ----
# [2026-09-23] Claude Code — deps(requirements): add onnxruntime, tokenizers, huggingface_hub for canonical ng_embed
# What: Declare runtime dependencies required by canonical ng_embed.py (ONNX runtime, HF fast tokenizer, HF hub client).
# Why: ng_embed.py is vendored ecosystem-wide; consumers need these packages installed or import fails.
# How: Added three pinned-minimum lines to requirements.txt immediately after transformers.
# -------------------
onnxruntime>=1.17.0  # canonical ng_embed: Snowflake-arctic-embed-m-v1.5 ONNX runtime
tokenizers>=0.19.0   # canonical ng_embed: HuggingFace fast tokenizer
huggingface_hub>=0.23.0  # canonical ng_embed: model/tokenizer download + optional HF remote inference
beautifulsoup4>=4.12.0
PyPDF2>=3.0.0
watchdog>=3.0.0
